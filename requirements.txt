# ── Web layer ─────────────────────────────────────────────
fastapi==0.115.4
uvicorn[standard]==0.31.0      # gunicorn alternative not needed on VPS
python-multipart==0.0.9        # FastAPI <-- file-upload parser
python-dotenv==1.0.1           # load .env locally; no secrets in repo

# ── Backend logic ─────────────────────────────────────────
PyMuPDF==1.24.12
google-cloud-vision==3.7.4
openai==1.55.3
pillow==10.2.0
python-docx==1.1.2
pdfservices-sdk==4.1.0

# ── (optional) Dev / test tools ──────────────────────────
# pytest==8.2.1
# httpx==0.27.0